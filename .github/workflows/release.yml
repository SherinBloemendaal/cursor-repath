name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
            cross: true
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install cross
        if: matrix.cross
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Build binary
        shell: bash
        env:
          TARGET: ${{ matrix.target }}
          USE_CROSS: ${{ matrix.cross }}
        run: |
          set -euo pipefail
          if [ "${USE_CROSS}" = "true" ]; then
            cross build --release --target "${TARGET}"
          else
            cargo build --release --target "${TARGET}"
          fi

      - name: Package archive (Unix)
        if: runner.os != 'Windows'
        shell: bash
        env:
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          stage="${RUNNER_TEMP}/crepath-stage"
          rm -rf "${stage}"
          mkdir -p "${stage}" dist
          cp "target/${TARGET}/release/crepath" "${stage}/crepath"
          chmod 755 "${stage}/crepath"
          cp README.md LICENSE DISCLAIMER.md "${stage}/"
          export COPYFILE_DISABLE=1
          tar -czf "dist/crepath-${TARGET}.tar.gz" -C "${stage}" \
            crepath README.md LICENSE DISCLAIMER.md

      - name: Package archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          TARGET: ${{ matrix.target }}
        run: |
          $ErrorActionPreference = 'Stop'
          $stage = Join-Path $env:RUNNER_TEMP 'crepath-stage'
          if (Test-Path $stage) { Remove-Item -Recurse -Force $stage }
          New-Item -ItemType Directory -Force -Path $stage | Out-Null
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item "target\$env:TARGET\release\crepath.exe" (Join-Path $stage 'crepath.exe')
          Copy-Item README.md, LICENSE, DISCLAIMER.md -Destination $stage
          $archive = "dist\crepath-$env:TARGET.zip"
          if (Test-Path $archive) { Remove-Item -Force $archive }
          Compress-Archive -Path @(
            (Join-Path $stage 'crepath.exe'),
            (Join-Path $stage 'README.md'),
            (Join-Path $stage 'LICENSE'),
            (Join-Path $stage 'DISCLAIMER.md')
          ) -DestinationPath $archive

      - name: Upload archive
        uses: actions/upload-artifact@v6
        with:
          name: crepath-${{ matrix.target }}
          path: dist/crepath-${{ matrix.target }}.${{ matrix.archive }}
          if-no-files-found: error
          retention-days: 1

  release:
    name: Publish release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download archives
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Write SHA256SUMS
        shell: bash
        run: |
          set -euo pipefail
          cd artifacts
          expected=(
            crepath-aarch64-apple-darwin.tar.gz
            crepath-x86_64-apple-darwin.tar.gz
            crepath-x86_64-unknown-linux-gnu.tar.gz
            crepath-aarch64-unknown-linux-gnu.tar.gz
            crepath-x86_64-pc-windows-msvc.zip
          )
          for name in "${expected[@]}"; do
            if [ ! -f "${name}" ]; then
              echo "missing release asset: ${name}" >&2
              ls -la
              exit 1
            fi
          done
          sha256sum "${expected[@]}" | LC_ALL=C sort -k2 > SHA256SUMS
          cat SHA256SUMS

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref, '-alpha') || contains(github.ref, '-beta') || contains(github.ref, '-rc') }}
          generate_release_notes: true
          files: |
            artifacts/crepath-aarch64-apple-darwin.tar.gz
            artifacts/crepath-x86_64-apple-darwin.tar.gz
            artifacts/crepath-x86_64-unknown-linux-gnu.tar.gz
            artifacts/crepath-aarch64-unknown-linux-gnu.tar.gz
            artifacts/crepath-x86_64-pc-windows-msvc.zip
            artifacts/SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
